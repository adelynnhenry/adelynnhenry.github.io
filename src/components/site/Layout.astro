---
import DocumentLayout from "../document/Layout.astro";
import Header from "./Header.astro";
const { site } = Astro.props;
const frontmatter = {
  title: null,
  description: null,
};
---

<DocumentLayout site={site} frontmatter={frontmatter}>
  <Header site={site} />
  <slot />
</DocumentLayout>
